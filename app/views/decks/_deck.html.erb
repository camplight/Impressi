<li>
	<%= deck.name %> <%= link_to "Show", deck_path(deck.id) %> <%= link_to "Edit", edit_deck_path(deck.id) %> <%= link_to "Delete", deck_path(deck.id), :confirm => "Are you sure?", :method => :delete %>
</li>