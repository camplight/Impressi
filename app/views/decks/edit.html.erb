<%= render :partial => 'decks/template_selector', :locals => { :template_data => @template_data, :deck => @deck } %>

<div id='container-custom'>
	<div id='impress' class='seven columns' deck_id="<%= @deck.id %>" data-mode = 'edit' style='z-index:-1'>
	</div>
</div>

<%= javascript_include_tag "pickle.js" %>
