<h1 id ="user-decks">Your Decks</h1>

<div class="decks">
	<% if @decks.nil? %>
	  You don't have any decks.
  <% else %>
		<table id="user-decks"><%= render :partial => 'decks/deck', :collection => @decks %></table>	 
  <% end %>
</div>

<%= render :partial => 'decks/create_deck', :locals => { :deck => @deck } %>
